<%= simple_form_for(:session, url: login_path) do |f| %>
    <%= f.label :name, 'Name ' %>
    <%= f.input_field :name,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "name" }%>
  <%= f.button :submit, "Log In"%>
<% end %>